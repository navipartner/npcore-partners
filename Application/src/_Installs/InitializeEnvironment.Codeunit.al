codeunit 6014694 "NPR Initialize Environment"
{
    Access = Internal;
    ObsoleteState = Pending;
    ObsoleteTag = '2024-02-28';
    ObsoleteReason = 'We ended up not using Sandbox NSTs for crane containers, so we do not need to auto disable Microsofts webservice protection for sandboxes';
}
