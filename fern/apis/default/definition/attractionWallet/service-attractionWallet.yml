# yaml-language-server: $schema=https://raw.githubusercontent.com/fern-api/fern/main/fern.schema.json

imports:
  simpleTypes: ./types-simple.yml
  compositeTypes: ./types-composite.yml
  globalApiTypes: ./../global-api-types.yml

service:
  display-name: Attraction Wallet
  auth: true
  base-path: /attractionWallet

  endpoints:

    createWallet:
      docs: 
        Create a new wallet.
        Note that in the `Create Wallet` and `Add to Wallet`, API calls, the assets may also be identified by their visual reference numbers. 
        If the assetId does not interpret as a UUID, the system will attempt to resolve it as a visual reference number.
      display-name: Create Wallet
      method: POST
      path: ""
      request:
        name: createWallet
        body: CreateWalletRequest
      response: compositeTypes.WalletDetails
      examples:
        - name: CreateWalletRequest
          path-parameters:
            tenant: $globalApiTypes.tenant.Exampletenant
            environment: $globalApiTypes.environment.ExampleenvironmentProduction
            company: $globalApiTypes.Company.ExampleCompanyCronus
          request: $CreateWalletRequest.CreateWalletRequest
          response:
            body: $compositeTypes.WalletDetails.ExampleWalletDetails


    addToWallet:
      docs: 
        Add assets to an existing wallet.
        Note that in the `Create Wallet` and `Add to Wallet`, API calls, the assets may also be identified by their visual reference numbers. 
        If the assetId does not interpret as a UUID, the system will attempt to resolve it as a visual reference number.
      display-name: Add to Wallet
      method: POST
      path: /{walletId}
      path-parameters:
        walletId: simpleTypes.WalletId
      request:
        name: addToWallet
        body: AddToWalletRequest
      response: compositeTypes.WalletDetails
      examples:
        - name: AddToWalletRequest
          path-parameters:
            tenant: $globalApiTypes.tenant.Exampletenant
            environment: $globalApiTypes.environment.ExampleenvironmentProduction
            company: $globalApiTypes.Company.ExampleCompanyCronus
            walletId: $simpleTypes.WalletId.ExampleWalletId
          request: $AddToWalletRequest.AddToWalletRequest
          response:
            body: $compositeTypes.WalletDetails.ExampleWalletDetails

    getWallet:
      docs: Get a wallet by its ID.
      display-name: Get Wallet
      method: GET
      path: /{walletId}
      path-parameters:
        walletId: simpleTypes.WalletId
      request:
        name: GetWallet
        query-parameters:
          withDetails: optional<simpleTypes.WithDetails>
          languageCode: optional<simpleTypes.LanguageCode>
      response: compositeTypes.WalletDetails
      examples:
        - name: Get Wallet with details
          path-parameters:
            tenant: $globalApiTypes.tenant.Exampletenant
            environment: $globalApiTypes.environment.ExampleenvironmentProduction
            company: $globalApiTypes.Company.ExampleCompanyCronus
            walletId: $simpleTypes.WalletId.ExampleWalletId
          query-parameters:
            withDetails: $simpleTypes.WithDetails.ExampleWithDetailsTrue
            languageCode: $simpleTypes.LanguageCode.ExampleLanguageCode_ENU
          response:
            body: $compositeTypes.WalletDetails.ExampleWalletDetailsWithDetails


    findWallet:
      docs: Find a wallet by any of its external references.
      display-name: Find Wallet
      method: GET
      path: ""
      request:
        name: findWallet
        query-parameters:
          referenceNumber: optional<simpleTypes.ExternalWalletReferenceNumber>
          referenceNumbers: optional<simpleTypes.ExternalWalletReferenceNumbers>
          withAssets: optional<simpleTypes.WithAssets>
          withDetails: optional<simpleTypes.WithDetails>
          languageCode: optional<simpleTypes.LanguageCode>
      response: list<compositeTypes.WalletDetails>
      examples:
        - name: Find Wallets with Assets including Details
          path-parameters:
            tenant: $globalApiTypes.tenant.Exampletenant
            environment: $globalApiTypes.environment.ExampleenvironmentProduction
            company: $globalApiTypes.Company.ExampleCompanyCronus
          query-parameters:
            referenceNumbers: $simpleTypes.ExternalWalletReferenceNumbers.ExampleExternalWalletReferenceNumbers
            withAssets: $simpleTypes.WithAssets.ExampleWithAssetsTrue
            withDetails: $simpleTypes.WithDetails.ExampleWithDetailsTrue
            languageCode: $simpleTypes.LanguageCode.ExampleLanguageCode_ENU
          response:
            body: 
              - $compositeTypes.WalletDetails.ExampleWalletDetailsWithDetails

    assetHistory:
      docs: Get the history of an asset in a wallet.
      display-name: Asset History
      method: GET
      path: /assetHistory/{assetId}
      path-parameters:
        assetId: simpleTypes.AssetId
      request:
        name: assetHistory
      response: compositeTypes.AssetHistory
      examples:
        - name: AssetHistory
          path-parameters:
            tenant: $globalApiTypes.tenant.Exampletenant
            environment: $globalApiTypes.environment.ExampleenvironmentProduction
            company: $globalApiTypes.Company.ExampleCompanyCronus
            assetId: $simpleTypes.AssetId.ExampleAssetId
          response:
            body: $compositeTypes.AssetHistory.ExampleAssetHistory

    confirmWalletPrint:
      docs: Confirm the print of a wallet.
      display-name: Confirm Print
      method: POST
      path: /{walletId}/confirmPrint
      path-parameters:
        walletId: simpleTypes.WalletId
      request:
        name: ConfirmWalletPrintRequest
      response: compositeTypes.WalletDetails
      examples:
        - name: ConfirmWalletPrintExample
          path-parameters:
            tenant: $globalApiTypes.tenant.Exampletenant
            environment: $globalApiTypes.environment.ExampleenvironmentProduction
            company: $globalApiTypes.Company.ExampleCompanyCronus
            walletId: $simpleTypes.WalletId.ExampleWalletId
          response:
            body: $compositeTypes.WalletDetails.ExampleWalletDetails

    clearConfirmWalletPrint:
      docs: Clears the lastPrintedAt field using the wallet id
      display-name: Clear Confirm Print
      method: POST
      path: /{walletId}/clearConfirmPrint
      path-parameters:
        walletId: simpleTypes.WalletId
      request:
        name: ClearConfirmWalletPrintRequest
      response: compositeTypes.WalletDetails
      examples:
        - name: ClearConfirmWalletPrintExample
          path-parameters:
            tenant: $globalApiTypes.tenant.Exampletenant
            environment: $globalApiTypes.environment.ExampleenvironmentProduction
            company: $globalApiTypes.Company.ExampleCompanyCronus
            walletId: $simpleTypes.WalletId.ExampleWalletId
          response:
            body: $compositeTypes.WalletDetails.ExampleWalletDetailsNoPrint

types:

  CreateWalletRequest:
    properties:
      name: optional<simpleTypes.Description>
      tickets: optional<list<simpleTypes.AssetId>>
      memberCards: optional<list<simpleTypes.AssetId>>
      coupons: optional<list<simpleTypes.AssetId>>
      vouchers: optional<list<simpleTypes.AssetId>>
      externalReferenceNumbers: optional<list<simpleTypes.ExternalWalletReferenceNumber>>
      originatesFromItemNo: optional<simpleTypes.OriginatesFromItemNo>
    examples:
      - name: CreateWalletRequest
        value:
          name: "My wallet holds my tickets, coupons and vouchers for the event."
          tickets: 
            - $simpleTypes.AssetId.ExampleTicketId1
          memberCards: 
            - $simpleTypes.AssetId.ExampleMemberCardId1
          coupons:
            - $simpleTypes.AssetId.ExampleCouponId1
          vouchers:
            - $simpleTypes.AssetId.ExampleVoucherId1
          externalReferenceNumbers: 
            - $simpleTypes.ExternalWalletReferenceNumber.ExampleExternalWalletReferenceNumber
          originatesFromItemNo: $simpleTypes.OriginatesFromItemNo.ExampleWalletNo


  AddToWalletRequest:
    properties:
      tickets: optional<list<simpleTypes.AssetId>>
      memberCards: optional<list<simpleTypes.AssetId>>
      coupons: optional<list<simpleTypes.AssetId>>
      vouchers: optional<list<simpleTypes.AssetId>>
    examples:
      - name: AddToWalletRequest
        value:
          tickets: 
            - $simpleTypes.AssetId.ExampleTicketId2
          memberCards: 
            - $simpleTypes.AssetId.ExampleMemberCardId2
          coupons:
            - $simpleTypes.AssetId.ExampleCouponId2
          vouchers:
            - $simpleTypes.AssetId.ExampleVoucherId2
