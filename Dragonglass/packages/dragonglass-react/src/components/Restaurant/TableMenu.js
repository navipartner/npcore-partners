import React, { PureComponent } from "react";

class TableMenu extends PureComponent {
    render() {
        return (
            <div>
                {/* This thing should slide in from the right part of the screen and take about 40% of the screen */}
            </div>
        );
    }
}
